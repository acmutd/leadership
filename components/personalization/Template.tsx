export default function customComponent() {
    return (<div>Welcome, copy this component and update anything inside this div. Then rename the component to your first + last name without any spaces.</div>);
}